		Mark connections where a SESSIONCONFIG has been applied to. Close those connections at the end of the patch to reset possible session changes.
		Add version deprecation
		Add test for branches and return patches
		When the current version is not found as source -> then it is deprecated?
		Add DONTCOUNT test
		Add test that works without a property file
		Replace driverjars by classpathextension option
		Must be able te skip the app configuration

NOT		Add potential target patch
DONE	Add -target wildcard option
			examples:
				-target *
				-target 1.0.*
DONE	Add "database is up to date"
DONE	Add test for open patches
